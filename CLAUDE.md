* **Infinite Flux Vibe** is the fast-track, vibe-coded version of Infinite Flux. Ship features quickly. Don't over-explain — just build.
* Move fast, use AI agents in parallel, prefer working code over perfect code.
* Update docs in docs dir after significant changes, but don't let docs slow down shipping.
* When a task can be split into independent pieces, run multiple agents in parallel.
* Prefer `cargo clippy` clean code and tests for critical systems, but don't block on 100% coverage.